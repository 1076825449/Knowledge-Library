# Web框架
# 注意：Xcode Python 3.9 环境（/Applications/Xcode.app）实际装的是 Flask 2.3.0。
# 此 requirements.txt 适配 Xcode Python 3.9 环境。
# 如果使用 hermes venv（python3 3.11）环境，请用 Flask==3.0.0 + Werkzeug==3.0.1。
Flask==2.3.0
Jinja2==3.1.3
Werkzeug==3.0.1
flask-cors==4.0.0

# 数据库
# sqlite3 为 Python 标准库，无需安装

# 测试
pytest==8.3.5

# 代码质量
# black==24.0.0  # 可选：代码格式化
# flake8==7.0.0  # 可选：linting
